# حسابیار خودش یه پروژه‌ی Node.js هست (بک‌اند Express + فرانت React/Vite)،
# پس هیچ پکیج پایتونی واقعی لازم نداره. این فایل فقط برای اینه که مرحله‌ی
# «pip install -r requirements.txt» توی رانر دسکتاپ خطا نده و اجرا بشه.
#
# تنها اسکریپت پایتونی پروژه، hesabyar_audit.py، فقط از کتابخانه‌ی استاندارد
# پایتون استفاده می‌کنه (argparse, json, urllib, ...) و نیازی به نصب چیزی نداره.
#
# اگه بعداً یه ابزار پایتونی با وابستگی واقعی به پروژه اضافه شد، همین‌جا اضافه‌ش کن.
